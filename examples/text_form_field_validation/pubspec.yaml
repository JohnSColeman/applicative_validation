name: text_form_field_validation

description: A Flutter project to demonstrate using applicative domain.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  applicative_validation:
    path: ../../
  flutter_bloc: ^8.0.0
  equatable: ^2.0.3
  easy_localization: ^3.0.0
  easy_localization_loader: ^1.0.0
  dartz: ^0.10.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - assets/translations/
  uses-material-design: true
